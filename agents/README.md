# Universal Agent Surface

This directory is the universal agent-role surface for all agent platforms.

Current canonical agent content is maintained in:
- `.claude/agents/`

Migrated universal entrypoints:
- `agents/database-reviewer.md`

Migration policy:
- Add new shared agent roles in this `agents/` surface over time.
- Keep `.claude/agents/` and this surface aligned during transition.
- Do not maintain multiple divergent copies.
